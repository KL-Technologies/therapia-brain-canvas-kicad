logs/*.tmp
logs/patch_variants/
.DS_Store
*.pyc
__pycache__/
~*.lck
board/~*.lck

# ODB++ is exported only as a third opinion on the net set (scripts/62_check_fab.py).
# It is not uploaded, it is 80 files of regenerable intermediate, and it carries
# creation timestamps that would churn on every export. board.d356 is kept: it is
# one file and JLC accepts it for flying-probe testing.
fab/odb/

# Working copies for the copper XOR and the analog diff: an extracted baseline
# board and four SVG plots, all regenerable from git in seconds. The images they
# produce (reports/copper_xor_*.png) are kept.
logs/copper_xor/
logs/analog_diff/*.kicad_pcb
